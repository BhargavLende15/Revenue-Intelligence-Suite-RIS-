services:
  - type: web
    name: revenue-intelligence-suite
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server --timeout 120 --workers 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
